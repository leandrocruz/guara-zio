shared = "/opt/guara/service/shared"

name = "Guara Server"

await = 30 seconds

http {
  port = 9013
}

jwt {
  key = ${shared}"/secret.key"
}

morbid {
  magic = ""
  url   = ""
  updateEvery = 20 minutes
}

kafka {
  key     = ""
  secret  = ""
  servers = [""]
  consumer {
    enabled = false
    topic   = ""
    group   = ""
  }
}
